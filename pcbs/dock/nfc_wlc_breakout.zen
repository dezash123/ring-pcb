MolexPicoBlade = Module("@stdlib/kicad/MolexPicoBlade.zen")

vin5 = io("vin5", Net)
gnd  = io("gnd", Net)

txp   = io("txp", Net)
txn   = io("txn", Net)
comm  = io("comm", Net)
vrect = io("vrect", Net)

MolexPicoBlade(
  name="J_WLC",
  variant="53047-0610_1x06_Vertical",
  Pin_1=vin5, Pin_2=gnd, Pin_3=txp, Pin_4=txn, Pin_5=comm, Pin_6=vrect
)
